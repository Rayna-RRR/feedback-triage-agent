# Project Brief

## 产品定义

发版后反馈风险工作台面向中小型 AI / SaaS 团队的产品经理和产品运营，用来识别某个问题在新版本发布后是否新增或加重，并把判断、用户原话、人工决定和后续处理放进同一个可追踪流程。

Feedback Triage Agent 是分析与审计引擎；Version Feedback Risk Radar 的 24 / 48 / 72 小时观察、来源筛选、问题卡片、复核队列和负责人摘要是主要操作界面。两者共享观察任务、反馈证据、问题簇和人工状态，不是两个独立 Demo 的跳转。

## 核心业务闭环

1. 创建版本观察任务，填写产品、基线版本、当前版本和发版变化摘要。
2. 定义基线与当前反馈窗口；默认要求同等长度，其他口径必须填写并展示说明。
3. 分别导入两个版本的反馈；默认使用累计快照，也支持增量导入，两种方式都去重并留存导入记录。
4. 校验并标准化字段；新版观察任务允许缺少 `rating`，不会伪造评分。
5. 将多条同类反馈聚合为问题簇，保留每条原始反馈的来源、窗口和反馈 ID。
6. 同时比较问题簇在两个窗口中的数量和反馈占比，展示新增、加重、稳定、缓解或证据不足。
7. 将高风险、低置信度、多意图、判断冲突或证据不足的问题交给人工复核。
8. 记录确认、驳回、保持观察、合并和拆分，以及负责人、处理状态、下一步动作和结果。
9. 在下一观察窗口重新导入真实数据，验证问题是缓解、持续还是恶化。

系统只描述“发版后新增或加重”，不据此断言问题由该版本导致。版本变化摘要只能帮助人工复核，不能替代原始反馈或其他因果证据。

## 目标用户与使用时机

目标用户：

- 中小型 AI / SaaS 团队的产品经理。
- 负责应用商店、客服、社区和访谈反馈的产品运营。
- 需要组织发版后问题复盘和负责人跟进的团队成员。

使用时机：

- 新版本发布后的 24 / 48 / 72 小时观察。
- 灰度或全量发布后的重复观察。
- 产品周会、发版复盘和问题负责人同步之前。

不适用：

- 证明版本改动与问题之间的因果关系。
- 替代埋点、崩溃分析、实验平台和正式工单系统。
- 需要登录、权限、多租户、合规归档或稳定云数据库的生产场景。
- 需要 RAG、向量检索、爬虫或外部平台真实写入的流程。

## 关键对象

### 观察任务

记录产品、两个版本、窗口、比较口径和版本变化摘要。默认同等窗口；非同等口径必须显式说明。

### 导入批次

记录基线或当前窗口、24 / 48 / 72 小时、来源、累计或增量方式、文件名、有效数、重复数、异常数和运行信息。

### 反馈证据

保存反馈 ID、来源、原文、可选评分、所属窗口、标准化结果、分类、优先级、置信度和复核原因。新版观察任务允许 `rating` 缺失。

### 问题簇

作为风险判断和人工处理的主要单位，保存分类、摘要、成员反馈、基线与当前数量和占比、变化状态、风险等级、依据、证据缺口和置信度。

### 人工决定与处理记录

保存确认、驳回、保持观察、合并、拆分和更新操作，以及操作者说明、负责人、工作状态、下一步动作、结果和时间。人工记录追加保存，不覆盖系统初始判断和原始反馈。

## 风险与状态边界

以下概念必须分开：

- 反馈级优先级：单条反馈的严重程度。
- 问题簇级风险：结合当前证据和前后变化后的风险等级。
- 变化状态：新增、加重、稳定、缓解或证据不足。
- 复核状态：待复核、已确认、已驳回或保持观察。
- 处理状态：待处理、处理中、待验证、已缓解或已关闭。

负责人摘要只汇总有原始证据且经过人工确认的问题。人工填写“已缓解”不能代替下一窗口数据验证。

## 审计要求

审计嵌入任务和问题簇，不新增孤立的审计页面。至少保留：

- 输入完整性、无效行、重复数据和导入方式。
- 结论对应的原始反馈证据。
- 规则与模型判断冲突、fallback 和异常。
- 风险等级依据、置信度和证据缺口。
- 人工修改前后的状态、说明和时间。
- 前后窗口的数量、占比和状态一致性。
- 页面摘要与底层数据的对应关系。
- 运行过程和人工操作的追加式留痕。

## 存储与部署边界

第一版不接数据库或付费服务。

- 本地观察任务使用 JSON 和文件目录持久化，默认保存在 `data/observation_tasks/`。
- 可以通过 `FEEDBACK_RISK_TASKS_DIR` 指定其他本地可写目录。
- Vercel 使用 `/tmp/feedback-risk-tasks/`，属于临时存储，重启或实例回收后可能丢失。
- 线上 Demo 不应上传隐私、商业秘密或生产反馈。

## 兼容的旧能力

旧 CLI、Ask 和固定七步 Agent 接口继续保留，用于单批次分诊、QA、报告和回归评测：

```text
load_feedback
→ validate_schema
→ classify_feedback
→ detect_badcases
→ generate_issue_cards
→ qa_check
→ export_report
```

兼容入口仍要求 `id`、`source`、`app_name`、`review_text`、`rating` 五个字段。缺少 `rating` 的容忍只适用于新版观察任务导入。

保留能力包括 CSV 标准化、规则分类、可选 LLM 初稿、规则 fallback、badcase、人工复核、QA 报告、run log、结构化结果、周报摘要和 golden set 评测。

## 测试与评测边界

运行自动化测试：

```bash
python -m pytest
```

运行规则评测：

```bash
python -m feedback_triage_agent.cli evaluate \
  --input data/evaluation_feedback.csv \
  --output data/evaluation_output
```

golden set 和对抗样本只用于小样本回归与 badcase 定位，不代表生产准确率、节省时间、真实用户规模或业务效果。文档和页面不硬编码测试通过数、CI 结果或演示指标。
